# Apostrophe // Multisite Mod

- Apostrophe 1.5

Notes:
- modified core files: 
	aPageTable.class (self::queryWithSlots => aPageTable::queryWithSlots) - unextendable by default, will be in next release as well